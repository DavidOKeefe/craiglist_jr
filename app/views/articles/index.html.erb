<h1>Articles</h1>
<%= button_to 'Make a new listing!', new_category_article_path(@category, @article), :method => "get" %>
<div class="all_articles">
  <% @articles.each do |article| %>
    <p><%= link_to article.name, [@category, article] %></p>
  <% end %>
</div>
